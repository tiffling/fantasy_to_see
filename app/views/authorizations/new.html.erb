<h1>Authorize</h1>
<%= link_to 'Click here', @url, target: 'blank' %>

<div class="authorizer-form">
  <%= form_tag(authorizations_path, method: :post) do %>
    <div>
      <label>Verifier Code</label>
      <%= text_field_tag :verifier %>
    </div>
    <br>
    <%= submit_tag 'Authorize' %>
  <% end %>
</div>
